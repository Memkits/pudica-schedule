# Calcit 0.13.29 upgrade

- Rebased onto current `main` and updated Calcit/procs and Respo dependency metadata to 0.13.29.
- Migrated the workflow and weak-type helper from the legacy runner to `setup-calcit` and `calcit`, and gated deployment to pushes on `main`.
- Kept `calcit.cirru` as the canonical snapshot and verified dependency resolution, immutable Yarn install, check-only, weak-type gate, and JS codegen.
